# Database URL.
#
# Local dev (default): file-based SQLite at ./app.db inside backend/.
# Production (Fly.io): set to sqlite:////data/app.db (mounted persistent volume).
# Tests: use sqlite:///:memory: via the conftest fixture, no env var needed.
DATABASE_URL=sqlite:///./app.db
